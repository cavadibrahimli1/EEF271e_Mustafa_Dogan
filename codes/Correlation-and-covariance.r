# Define a vector of values for two random variables
x <- c(1, 2, 3)
y <- c(4, 5, 6)

# Calculate the correlation between the two random variables
cor(x, y)

# Calculate the covariance between the two random variables
cov(x, y)
